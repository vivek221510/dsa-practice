// 1000. Longest Subsequence With Non-Zero Bitwise XOR
// Description: Find the length of the longest subsequence whose bitwise XOR is non-zero.

class Solution {
    public int longestSubsequence(int[] nums) {
        long xor=0;
        boolean val=true;
        for(int i=0;i<nums.length;i++)
            {
                xor^=nums[i];
                if(nums[i]!=0) val=false;
            }
        if(val)return 0;
        if(xor!=0)return nums.length;
        return nums.length-1;
    }
}

/*
Example:
Input: nums = [2,3,1,3]
Output: 4

Time Complexity: O(n)
Space Complexity: O(1)
*/
